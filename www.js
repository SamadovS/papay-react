<Button style={{ background: "rgba(247, 9, 9, 0.72)" }} variant="contained">
  Bekor qilish subscribeHandler
</Button>;

<Tab
  style={{ flexDirection: "column" }}
  value={"4"}
  component={() => (
    <Button variant={"contained"} onClick={() => setValue("4")}>
      Maqola Yozish
    </Button>
  )}
/>;
